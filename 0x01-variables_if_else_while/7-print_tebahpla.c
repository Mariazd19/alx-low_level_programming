#include <stdio.h>

/**
 * main - Prints the lowercase alphabet in reverse.
 *
 * Return: Always 0.
 */
int main(void)
{
char letter;
for (lettre = 'z'; lettre >= 'a'; lettre--)
putchar(lettre);
putchar('\n');
return (0);
}
